# 开发需求与实现状态

## 项目目标

`chAngE_Prism` 用于扩展 Prism 2 的镜头制作流程，将服务器镜头初始化、审片媒体处理、DCC 文件归档和日常文件投递集中到 Project Browser。

当前版本：`v2.2.0`。

正式验证环境：

- Windows
- Prism 2.1.2 / 2.1.3
- Nuke Archive 核心：Nuke 13.2 / Python 3.7
- Houdini Archive：Houdini 20.5+

## 功能状态

| 功能 | 状态 | 说明 |
|---|---|---|
| 服务器镜头批量导入 | 已实现 | 扫描、筛选、创建/更新项目和镜头、部门、帧范围、review MOV |
| 打开服务器地址 | 已实现 | 镜头右键打开 Animation、Cloth、Hair 和 Shot Root |
| ACES / OCIO 转换 | 已实现 | RGB EXR 转 H.264 MP4 / ProRes MOV |
| Nuke Archive | 已实现 | 标准 Read 收集、相对路径归档、版本浏览、健康状态、详情和删除 |
| Houdini Archive | 已实现 | 单次 hython 依赖扫描、内存路径改写和 Save As，退出后按 manifest 后台复制，并统一 Archive 浏览 |
| Daily Review Copy | 已实现 | 文件或 Media 序列复制到当天审片目录 |
| 多镜头批量导出 | 未实现 | 将多个镜头整理为对外文件夹 |
| 其他 Nuke 依赖类型 | 未实现 | DeepRead、ReadGeo、Precomp、字体、Gizmo、OCIO 等 |

## 服务器镜头批量导入

用户根据服务器项目和 filter 获取镜头并创建本地 Prism 镜头。

Filter 示例：

```text
Q2EP007/SC01/shot001
SC01/shot001
shot001
```

支持逗号和换行分隔多个 filter。

服务器目录：

```text
{server_root}/{project}/publish/shot/{episode}/{sequence}/{shot}/{step_category}/{step_code}/
```

映射：

| 服务器 step | Prism 内容 |
|---|---|
| `shot_motion/shot_animation` | Animation |
| `shot_solution/cloth_solution` | Cloth |
| `shot_solution/hair_solution` | Hair |

当前行为：

- 服务器项目选择和镜头筛选。
- 创建或打开 Prism 项目。
- 创建新镜头的 FX、Lighting、Compositing 部门和预设场景。
- 已有镜头只更新 frame range 和 metadata。
- 从 `shot_animation/xml/description.xml` 读取 frame range，失败时回退 `[1001, 1100]`。
- `.mov` 通过 Prism media `review` identifier 创建版本。
- 其余服务器文件不复制、不记录。
- shot metadata 保存服务器项目、场景和镜头名，用于右键打开服务器路径。
- 批量失败收集镜头路径和异常，结束时统一显示。

## ACES / OCIO 转换

入口：

- `chAngE > ACES / OCIO Media Converter...`
- Media 预览右键 `ACES / OCIO Quick Convert`

当前范围：

- 输入为包含标准 RGB 通道的 EXR 单帧或序列。
- OCIO config：项目覆盖 → `OCIO` 环境变量 → `ocio://default`。
- 输出 H.264 MP4、ProRes 422 HQ MOV。
- 写入 BT.709 标签并对正式输出做首帧解码验证。

完整说明见 [ACES转换器.md](ACES转换器.md)。

## Nuke Archive

入口：

- Scenefiles 中 `.nk` 右键 `Package Nuke Archive...`
- Project Browser `Archives` 页签

当前范围：

- 每次打包单个 Nuke 文件。
- 纯文本解析标准 `Read {}`，核心不依赖 Nuke 或 Prism API。
- 图片序列复制整个源目录，MOV/单图只复制文件。
- 同源目录复用；同名不同源追加 `_2`、`_3`。
- 只修改归档副本中的 Read 和 Root `project_directory`。
- 每个 Task 独立 `v####` 版本，写入 `Archives/<task>/v####`；Department 只作为元数据，不参与版本编号；失败或取消清理新版本。
- 预检显示 Read、文件数、素材容量、磁盘空间和路径映射。
- Archive 页签显示健康状态和 Version Details。
- `Open Nuke` 走 Prism 的 DCC 启动配置。
- 每个版本可以二次确认后永久删除整个 `v####` 目录。

完整说明见 [Nuke Archive.md](Nuke%20Archive.md)。

## Houdini Archive

入口：

- Scenefiles 中 `.hip/.hiplc/.hipnc` 右键 `Package Houdini Archive...`
- Project Browser `Archives` 页签

当前范围：

- Houdini 20.5+；完全相同 build 优先，只允许同 major/minor 的更高 build 回退。
- 打包 ABC、FBX、非 File Cache VDB、OBJ/GEO/PLY/STL、纹理/HDRI、LUT、音频及其序列。
- File Cache 内部引用和全部 `.bgeo/.bgeo.sc` 固定归为 `Skipped Cache`。
- USD/Solaris 和 PDG/TOP 动态依赖归为 `Skipped Unsupported`。
- 外部 HDA/OTL 复制到 Archive，激活方式为手动，不修改 `HOUDINI_OTLSCAN_PATH`。
- 当前系统不可访问的 `/mnt/nas` 输入会标为 `Skipped Missing` 并保留原路径；其他缺失输入、未知外部引用、不可安全重写的锁定/动态参数会阻止打包。
- 原始 HIP 不修改；Prism 只启动一次 hython，在内存中重写后 Save As 并输出 `Incomplete` manifest。
- hython 退出后，普通 Python 根据 manifest 后台复制依赖；不显示预检或进度窗口，结束时弹窗报告。
- Nuke/Houdini 共用 `Archives` 浏览器，但仅按 Task 独立编号；旧 `Archives/v####` 和 `Archives/<department>/<task>/v####` 继续兼容。

完整说明见 [Houdini Archive.md](Houdini%20Archive.md)。

## Daily Review Copy

入口：

- Project Browser 文件右键
- Media 预览右键

目标：

```text
<review_copy.destination_root>/YYYY-MM-DD/
```

目录选择复制整个目录；文件选择复制单文件。目录采用合并覆盖，不删除目标中已有的额外内容。失败项在本次复制结束后统一显示。

## 配置

插件根目录 `config.json`：

```json
{
  "server_root": "P:/",
  "local_projects_root": "D:/Projects",
  "review_copy": {
    "destination_root": "E:/daily_review"
  }
}
```

OCIO 项目覆盖也存储在该配置中，由转换器界面维护。

## 架构约束

Prism 入口文件保留在 `Scripts/` 根层。业务放在：

```text
Scripts/change_prism/<feature>/
```

- `controller.py`：Prism API、callback、菜单和任务编排。
- `service.py`：不依赖 UI 的业务逻辑。
- `dialog.py`：Qt 界面和用户交互。
- `cli.py`：需要独立执行的命令行入口。

`Prism_chAngE_Prism_Functions.py` 必须保持为薄回调门面。feature 之间不能直接导入内部实现；共享配置和基础设施放在 `change_prism/` 根层。

## 测试与验收

```powershell
python -m unittest discover -s tests -v
```

最近一次 Prism PySide6 环境完整扫描发现 85 项测试：通过 79 项、跳过 6 项环境测试。

Nuke 13 可选验证：

```powershell
& "C:\Program Files\Nuke13.2v1\Nuke13.2.exe" --safe -t tests\nuke13_archive_smoke.py
```

需要有效 Nuke 许可证，测试仅使用临时小型素材。

Houdini 20.5/21.0/22.0 可选验证：

```powershell
& "C:\Program Files\Side Effects Software\Houdini 20.5.684\bin\hython.exe" tests\houdini_archive_smoke.py
& "C:\Program Files\Side Effects Software\Houdini 21.0.631\bin\hython.exe" tests\houdini_archive_smoke.py
& "C:\Program Files\Side Effects Software\Houdini 22.0.368\bin\hython.exe" tests\houdini_archive_smoke.py
```

需要有效 Houdini Batch/FX 许可证，测试使用临时小素材，不写真实镜头。

## 参考资料

- [README.md](README.md)：项目使用与功能总览
- [CLAUDE.md](CLAUDE.md)：代码架构和维护约定
- [prism环境变量.md](prism环境变量.md)：Prism 环境变量速查
- [prism_docs/](prism_docs/)：Prism 官方文档镜像
- 当前 Windows Prism 源码：`D:\pipeline\Prism\Prism_v2.1.3\Scripts`
